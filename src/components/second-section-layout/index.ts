import { Root } from './root';
import { SideImage } from './side-image';
import { Statistics } from './statistics';
import { Wrapper } from './wrapper';

export const SecondSectionLayout = {
  Root: Root,
  SideImage: SideImage,
  Wrapper: Wrapper,
  Statistics: Statistics,
}
